spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taxmanager_test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ci-test-secret-key-min-256-bits-para-github-actions-pipeline!!
  access-token-expiration: 900000
  refresh-token-expiration: 604800000